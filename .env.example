# Anthropic API key for Claude text generation
ANTHROPIC_API_KEY=sk-ant-your-key-here

# OpenAI API key for GPT text generation + DALL-E image generation
OPENAI_API_KEY=sk-your-key-here

# Google Gemini API key (optional — enables Gemini text + image generation)
GEMINI_API_KEY=your-gemini-key-here

# xAI API key for Grok text generation (optional)
XAI_API_KEY=xai-your-key-here

# YouVersion Platform API key for Bible verse fetching (optional — enables accurate NIrV scripture in Bible tier)
BIBLE_API_KEY=your-youversion-key-here

# --- Image Model Notes ---
# DALL-E (via OpenAI): Uses OPENAI_API_KEY above — always available when OpenAI is configured
# Gemini Images: Uses GEMINI_API_KEY above — enables Gemini as an image model option
# When both OPENAI_API_KEY and GEMINI_API_KEY are set, an image model selector appears on the start form
